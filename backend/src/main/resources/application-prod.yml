spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://tuitionhub-db:3306/tuitionhub?createDatabaseIfNotExist=true}
    username: ${DB_USERNAME:tuition_user}
    password: ${DB_PASSWORD:tuition_password}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:servicelaundry98@gmail.com}
    password: ${MAIL_PASSWORD:zlwbklpdnbibcvyg}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

app:
  jwt:
    secret: ${JWT_SECRET}
    expiration: 86400000
  razorpay:
    key-id: ${RAZORPAY_KEY_ID}
    key-secret: ${RAZORPAY_KEY_SECRET}

logging:
  level:
    root: INFO
    com.tuitionhub: INFO
